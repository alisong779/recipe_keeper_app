<!DOCTYPE html>
<html>
  <body>
    <form action="/recipes" method="post">
      <div class="form-group"><strong>Recipe Name:</strong> <input type="text" name="recipe_name" id="recipe_name" size="30" required></div>
        <br>
          <strong>Ingredients:</strong><br />
          <textarea name="ingredients" cols="40" rows="5" id="ingredients" required></textarea>
        </br>
        <br>
          <strong>Instructions:</strong><br />
          <textarea name="instructions" cols="40" rows="5" id="instructions" required></textarea>
        </br>
        <br>
         <div class="form-group"><strong>Category (select one):</strong> <br>
        <br>
        <% Category.all.each do |category| %>
          <input type="radio" name="recipe[category_id]" value="<%= category.id %>" id="category_<%= category.id %>" class="radio-inline" required>
         </div> 
     <% end %></br>
        <input type="submit" value="Add Recipe">
    </form>
  </body>
</html>
